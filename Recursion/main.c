//Some of n natural no.
//sum(10)=sum+sum(9)

#include<stdio.h>

int main()
{
	int k;
	k=sum(3);
	printf("%d\n",k);

}

int sum(int k)
{
	int s;
	if(a==1)
	{
		return a;
		s=a+sum(a-1);
		return s;

	}

}
